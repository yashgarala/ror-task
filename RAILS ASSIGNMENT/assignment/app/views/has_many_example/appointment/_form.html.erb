<%=form_with model:appointment, url:{action: "create"} do |f|%>
    
    <%=f.select(:physician_id,@physicians.collect{|d| [d.name,d.id]})%>
    <%=f.select(:patient_id,@patients.collect{|d| [d.name,d.id]})%>
    
<%= f.date_select :appointment_date %>
    <%=f.submit%>
    
<%end%>